import { StyleSheet, Text, View } from "react-native";
import React from "react";
import TimelineCalendarScreen from "../../components/CalendarScreens/CalendarTimeline";
import CalendarScreenTabs from "../../components/CalendarScreens/CalendarScreen";

const Events = () => {
  return <CalendarScreenTabs />;
};

export default Events;

const styles = StyleSheet.create({});
